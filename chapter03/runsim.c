#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
int main(int argc,char **argv){
	int pr_limit;
	int pr_count=0;
	if(argc!=2){
		fprintf(stderr,"Usage: %s n",argv[0]);
		return 1;
	}
	while(fget)
}
